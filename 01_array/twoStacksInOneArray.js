//implement two stacks in one array A[1...n] in such a way that neither stack overflows 
//unless the total number of elements in both stacks together in n. 
//The PUSH and POP operations should run in O(1) time. 

